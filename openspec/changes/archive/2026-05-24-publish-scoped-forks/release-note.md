## Release Note

This fork publishes StepCI and Runner under the `@steve.clogic` npm scope.

Packages:

- `@steve.clogic/runner` at `2.0.7-steve.0`
- `@steve.clogic/stepci` at `2.8.2-steve.0`

Highlights:

- StepCI now depends on `@steve.clogic/runner`.
- Local development is supported through `npm link` between the forked repositories.
- Tester install and verification guidance is included in the repository documentation.